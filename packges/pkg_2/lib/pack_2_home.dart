import 'package:flutter/material.dart';

class Pack2Home extends StatelessWidget {
  const Pack2Home({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Pack2Home", style: TextStyle(color: Colors.amber),)));
  }
  
}