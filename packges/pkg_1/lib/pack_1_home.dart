import 'package:flutter/material.dart';

class Pack1Home extends StatelessWidget {
  const Pack1Home({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Pack1Home", style: TextStyle(color: Colors.red),)));
  }
  
}